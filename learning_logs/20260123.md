## 반복문

### 반복문 종류
  1. for
  2. for-in (향상된 for문)
  3. for-of (향상된 for문)
  4. while (자바랑 같음)

## 함수(function)

### 함수의 정의
  - 다양한 작업을 하나의 단위로 묶어놓은 코드블록

### 함수의 정의 및 사용
1. 함수 선언식

```js
function 함수명(매개변수1, 매개변수2){
  실행문;
  (리턴);
}
```

2. 변수에 함수
  - 변수에 let을 쓰고 함수를 넣으면 순서대로 실행됨 
  - 변수에 함수를 넣기 전에 함수를 사용하면 에러뜸

3. 함수 표현식(arrow function)

4. Function 생성자 함수
```js
let sum = new Function('a', 'b', 'return a + b;');
let sum1 = sum(1,2)
```

### 함수를 사용하는 이유
  1. 코드의 재사용성이 높음
  2. 유지보수성이 높음
  3. 가독성이 높음

### JS 내장 객체
  - 내장 객체에 있는 다양한 함수+ 사용자 정의 함수를 사용하면 가독성이 높아짐

### Object 객체
  - String / Number / Date / Array / Math 등 다양한 객체가 존재함

```js
let person = new Object();
person.fName = '영';
person.lName = '김';

person.getFullName = function(){
  return this.lName + this.fName;
}
console.log(person.getFullName());
```

### String 객체
1. length
2. indexOf() - 문자열 내에 특정 문자열이 존재 하는지 찾음 있으면 인덱스 리턴 없으면 -1 리턴
3. lastIndexOf() 
4. slice() - 음수 가능
5. substring() - 음수 불가능
6. substr() - 요즘은 안 쓰지만 시험문제에 나옴
7. replace() - 정규식 /영어문자열/i 도 있음
8. toUpperCas()
9. toLowerCase()
10. concat() - 2개 이상의 문자열을 하나로 합치는 함수임 (중요함)
11. trim() - 공백 삭제 (중요함)